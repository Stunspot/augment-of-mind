# Standalone synchronization boundary

The standalone repository preserves its GitHub Pages site and repository policy files. The installable MIND package source is synchronized byte-for-byte from the verified flagship `plugins/augment-of-mind` tree. Stale version-specific development artifacts are not carried forward as current evidence.
